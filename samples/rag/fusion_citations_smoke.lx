// Semantic fusion with citations smoke

set_default_model("simulated");

let arr = load_json("samples/rag/fixtures/fusion.json");
let fused = rag__fuse_passages_semantic(arr, 2, 0.7, true);
print(fused);


